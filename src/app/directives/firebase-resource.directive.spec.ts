import { FirebaseResourceDirective } from './firebase-resource.directive';

describe('FirebaseResourceDirective', () => {
  it('should create an instance', () => {
    const directive = new FirebaseResourceDirective();
    expect(directive).toBeTruthy();
  });
});
